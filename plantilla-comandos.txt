const Discord = require("discord.js"); 

module.exports = class reload {
    constructor() {
        this.name = "reload",
        this.alias = [],
        this.usage = "a!reload"
    }

    async run(client, message, args) {
        try{
        
        }catch(e) {
            throw e;
        }
    }
}